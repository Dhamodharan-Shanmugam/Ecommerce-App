import { CustomeClickDirective } from './custome-click.directive';

describe('CustomeClickDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomeClickDirective();
    expect(directive).toBeTruthy();
  });
});
